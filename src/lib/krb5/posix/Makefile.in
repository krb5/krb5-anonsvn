CFLAGS = $(CCOPTS) $(DEFS)

##DOSBUILDTOP = ..\..\..
##DOSMYNAME=posix
##DOSOBJFILE=..\$(MYNAME).lst

.c.o:
	$(CC) $(CFLAGS) -c $(srcdir)/$*.c
@SHARED_RULE@

MACSRCS = getuid.c strcasecmp.c strdup.c

OBJS = setenv.$(OBJEXT)	@LIBOBJS@

all-unix:: shared $(OBJS)
all-mac: $(OBJS)
all-windows:: $(OBJFILE)

##DOS$(OBJFILE): syslog.obj sscanf.obj
##DOS	$(RM) $(OBJFILE)
##DOS	$(LIBECHO) -p $(MYNAME)\ *.obj > $(OBJFILE)

shared:
	mkdir shared

clean-unix::
	$(RM) shared/*

clean-mac::
clean-windows::
	$(RM) $(OBJFILE)

